<h1>Studio <%= @studio.name %></h1>
<p>This studio belongs to <%= current_user.first_name %>.</p>
<p><%= @studio.address %></p>
<p><%= @studio.description %></p>

<%= link_to 'Delete Studio', @studio, method: :delete %>
<%= link_to 'Create Event', new_studio_event_path(@studio) %>
<%= link_to 'See Events for this Studio', studio_events_path(@studio) %>
